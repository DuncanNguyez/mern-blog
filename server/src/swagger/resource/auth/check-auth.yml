get:
  summary: check authentication
  description:
  responses:
    "200":
      content:
        application/json:
          schema:
            type: object
            properties:
              message:
                type: string
                example: authenticated
